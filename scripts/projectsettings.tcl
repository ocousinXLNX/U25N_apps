
# if a custom project name has been used (different from the directory name)
#set ProjectName fpga

# if a custom output folder should be used (different from the ./out folder)
#set DynamicOutputDir [pwd]/out

# any settings to be applied before running implementation, after loading static and dynamic dcp (custom contstraints etc)
#proc custom_settings() {} {
#}

# if an extra opt_design step is needed, with specific options etc
#proc custom_opt_design() {} {
#	opt_design -hier_fanout_limit 512
#}

